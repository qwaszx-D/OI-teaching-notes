# 对拍示例题：区间加，区间求和

给定一个长度为 `n` 的数组 `a`，有 `m` 次操作：

- `1 l r x`：把 `a[l]...a[r]` 都加上 `x`。
- `2 l r`：询问 `a[l]...a[r]` 的和。

## 输入格式

```text
n m
a1 a2 ... an
接下来 m 行，每行一个操作
```

## 输出格式

对每个 `2 l r` 操作，输出一行答案。

## 说明

- `fast.cpp` 用线段树实现。
- `brute.cpp` 用朴素数组实现。
- `gen.cpp` 随机生成小数据，用来对拍。
